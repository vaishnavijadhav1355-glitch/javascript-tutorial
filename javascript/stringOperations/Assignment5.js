const strPara = "Small efforts today lead to big achievements tomorrow";

const convertArrayWords = (str) => {
    return str.split(" ");
};

const arrayOfWords = convertArrayWords(strPara);

console.log(arrayOfWords);